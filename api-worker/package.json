{
  "name": "imgn-api-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "dependencies": {
    "itty-router": "^4.0.27"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "@cloudflare/vitest-pool-workers": "^0.8.11"
  }
}
